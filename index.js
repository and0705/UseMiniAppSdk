/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Cover from './Cover';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Cover);
